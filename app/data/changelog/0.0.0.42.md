---
version: 0.0.0.42
date: 2026-09-06
---

## TUI 重写：1:1 照抄 opencode 布局

- 无顶栏，全屏纯文本消息流
- 用户消息、助手文本 + 工具行（$ 命令、✱ 搜索、→ 读取等图标）
- 工具输出折叠 + Click to expand/collapse（鼠标支持）
- 底部：会话输入 / 模式行 Build·model / 分隔线 / 状态栏 cwd agents Ctrl+p commands 版本
- Tab 切换 BUILD/PLAN，Ctrl+C 两次退出（运行中按一次即停止）
- /commands 斜杠菜单、单击 /set 设置

## 平台

linux/amd64 linux/arm64 linux/386 windows/amd64 windows/386 windows/arm64 darwin/amd64 darwin/arm64 freebsd/amd64
